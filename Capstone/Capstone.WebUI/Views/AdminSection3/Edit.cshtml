@model Capstone.Domain.Entities.Section3

@{
    ViewBag.Title = "Edit";
}

<h1>Edit Section 3, @Model.pNight.Date</h1>

@using (Html.BeginForm("Edit", "AdminSection3Controller")) {

    @Html.EditorForModel()
    <input type="submit" value="Save" />

    @Html.ActionLink("Cancel and return to List", "Index")
}